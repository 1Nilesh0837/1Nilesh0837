name: Generate Profile Banner

on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - "scripts/banner/**"
      - "assets/source/**"
      - ".github/workflows/banner.yml"

permissions:
  contents: write

jobs:
  banner:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install dependencies
        run: pip install -r scripts/banner/requirements.txt

      - name: Pre-compute point data
        run: python scripts/banner/generate.py --precompute

      - name: Generate banners
        run: python scripts/banner/generate.py

      - name: Commit updated assets
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add assets/banner-*.svg scripts/banner/data/ scripts/banner/logos/
          if git diff --cached --quiet; then
            echo "No changes to commit"
          else
            git commit -m "chore: regenerate profile banners [skip ci]"
            git push
          fi
